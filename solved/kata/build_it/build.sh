#!/usr/bin/env bash

# activate a virtual environment.

# pip install the requirements. In this case, expect them to all be development requirements

# "compile" with python. This should only fail on syntax errors.

# run linter. The linter puts out a list of warnings that rarely can be completely eliminated.

# Advanced- count the lines of the output file and fail on more than 10 lines.

# run tests. If these fail, the build should fail.

# package for distribution
